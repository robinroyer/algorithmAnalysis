import pandas as pd
import sys
import numpy as np

test = pd.read_csv('tp1-H10-donnees/testset_1000_0.txt', sep = '/n')
a=np.array(test)
listtest=[]
for i in a :
    listtest.append(i[0])
print(listtest)
